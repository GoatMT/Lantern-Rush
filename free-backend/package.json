{
  "name": "lantern-rush-free-backend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "build": "wrangler deploy --dry-run --outdir .build",
    "deploy": "wrangler deploy",
    "test:runtime": "node runtime-check.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
