const KEY='lsl-account-v1';
const read=()=>{try{return JSON.parse(localStorage.getItem(KEY)||'{}');}catch{return {};}};
const esc=value=>String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const formatSeconds=s=>s==null?'—':`${Math.floor(s/60)}:${String(Math.floor(s%60)).padStart(2,'0')}`;
const account=read(),trophies=account.trophies||[],records=account.records||{};
const trophyList=[['first-match','FIRST MATCH','Complete your first match.'],['first-win','FIRST WIN','Win a match.'],['hot-streak','HOT STREAK','Win three matches in a row.'],['clean-sheet','SHUTOUT','Win without conceding.'],['rivalry-win','RIVALRY NIGHT','Win a rivalry fixture.'],['tournament-win','TOURNAMENT RUN','Win an Inter-Madrasah fixture.'],['season-win','SEASON CAMPAIGN','Win a season fixture.'],['dream-win','DREAM BUILDER','Win a Dream F.C. CPU match.']];
const trophyMarkup=()=>trophyList.map(([id,title,description])=>{const earned=trophies.some(t=>t.id===id);return `<article class="trophy-card ${earned?'earned':'locked'}"><span>${earned?'✦':'○'}</span><div><strong>${title}</strong><small>${description}</small></div><b>${earned?'EARNED':'LOCKED'}</b></article>`;}).join('');
const recordMarkup=(label,value)=>`<article class="record-card"><small>${label}</small><strong>${value||'—'}</strong></article>`;
const render=()=>{const root=document.getElementById('account-app');root.innerHTML=`<header class="account-top"><a href="./index.html" class="back">← LANTERN RUSH</a><span class="eyebrow">LOCAL ACCOUNT · FIREBASE READY</span></header><main class="account-main"><span class="eyebrow">YOUR MATCHDAY HISTORY</span><h1>ACCOUNT</h1><p class="account-lede">Your trophies and records are saved on this device now and can migrate to Firebase when accounts go live.</p><nav class="account-tabs"><a href="#trophies">TROPHIES</a><a href="#records">RECORDS</a></nav><section id="trophies" class="account-section"><div class="section-head"><div><span class="eyebrow">COLLECTED HONOURS</span><h2>TROPHIES</h2></div><b>${trophies.length}/${trophyList.length}</b></div><div class="trophy-grid">${trophyMarkup()}</div></section><section id="records" class="account-section"><div class="section-head"><div><span class="eyebrow">YOUR BEST MOMENTS</span><h2>RECORDS</h2></div><b>${account.matches||0} MATCHES</b></div><div class="record-grid">${recordMarkup('FASTEST GOAL',records.fastestGoal?`${formatSeconds(records.fastestGoal.seconds)} · ${records.fastestGoal.name}`:'—')}${recordMarkup('MOST GOALS IN A GAME',records.mostGoalsGame?`${records.mostGoalsGame.goals} · ${records.mostGoalsGame.name}`:'—')}${recordMarkup('BIGGEST WIN',records.biggestWin?`${records.biggestWin.score} · ${records.biggestWin.team}`:'—')}${recordMarkup('LONGEST WINNING STREAK',records.longestWinningStreak?`${records.longestWinningStreak} MATCHES`:'—')}</div></section></main>`;};
render();
