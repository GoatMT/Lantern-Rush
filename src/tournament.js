export function tournamentMatches(t){return t?.matches||[]}
export function tournamentRounds(t){return t?.playoffs?.rounds||[]}
export function tournamentStandings(t,divisionId){const d=(t?.divisions||[]).find(x=>x.id===divisionId)||(t?.divisions||[])[0];if(!d)return [];const rows=new Map((d.teams||[]).map(x=>[x.id,{teamId:x.id,name:x.name,gp:0,w:0,d:0,l:0,gf:0,ga:0}]));for(const m of tournamentMatches(t).filter(m=>(m.round||'Group Stage')==='Group Stage'&&m.divisionId===d.id)){if(!Number.isFinite(m.homeScore)||!Number.isFinite(m.awayScore))continue;for(const [id,gf,ga] of [[m.homeTeamId,m.homeScore,m.awayScore],[m.awayTeamId,m.awayScore,m.homeScore]]){const r=rows.get(id);if(!r)continue;r.gp++;r.gf+=gf;r.ga+=ga;if(gf>ga)r.w++;else if(gf<ga)r.l++;else r.d++;}}return [...rows.values()].map(r=>({...r,gd:r.gf-r.ga,pts:r.w*3+r.d})).sort((a,b)=>b.pts-a.pts||b.gd-a.gd||b.gf-a.gf)}
